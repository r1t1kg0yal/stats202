---
class: context-extraction
topic: 08 — dashboards_hub.md verbatim (the on-demand kernel)
expected_reply: ~28 KB
sent:
status: OPEN
send_only_if: prompt 01 classifies dashboards_hub.md as DIFFERS
---

**Staging-side note — do NOT paste this header into PRISM.**

Sized to sit alone: my copy is 27,113 B / 387 lines newline-stripped, sha
`7c2039c00269`, and the first measurement suggests PRISM's differs. It is the
largest single file in the series that still fits one reply, so it gets its own
prompt with nothing else attached.

---

## Paste everything below into PRISM

You are being asked to print one file from your own repository verbatim. Pure
introspection: do not build anything, do not report frictions, do not comment on
the content.

**Reply budget: this file is about 27 KB, which is close to the 30,000-character
ceiling, so send nothing else with it.** If it does not fit, stop at a heading
boundary, state the exact line number you stopped at, and I will ask for the
remainder in a follow-up.

Paste `prism-core/context/modules/static/tools/dashboards_hub.md` in full, in one
fenced block, exactly as it exists on disk — every blank line, every trailing
space, every code fence. Do not reflow, do not fix typos, do not normalise
whitespace, do not add or remove a trailing newline.

Head it with a single line: byte count, line count, and `sha256[:12]`, measured
with any single trailing newline stripped.

---

If something cannot be answered, add a short `## Could not resolve` section at
the end naming what you tried and what blocked it.
