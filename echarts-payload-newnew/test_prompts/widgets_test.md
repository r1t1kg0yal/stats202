Build a `bond_screener_popup` dashboard for 30 bonds from this exact user-supplied verified call: `bonds=bond_client.get_screen(universe="USD_IG", limit=30, fields=["cusip","ticker","rating","spread_bp","ytm_pct","price","price_1y_low","price_1y_high"])`, returning those columns as `string,string,string,float64,float64,float64,float64,float64`; persist with `save_artifact(bonds, name="bonds", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`. Then call exactly `bond_history=bond_client.get_history(cusips=bonds["cusip"].tolist(), start="-24M", fields=["date","cusip","price"])`, returning `date:string,cusip:string,price:float64`, and persist with `save_artifact(bond_history, name="bond_history", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`. On tab id `screen`, full-width `data_grid` id `bond_table` uses `page_size=50` and `h_px=640`, shows ticker/rating/spread/ytm, and declares a rich `row_click.detail.sections` list: `{"type":"chart","id":"bond_history_popup","dataset":"bond_history","row_key":"cusip","filter_field":"cusip","mapping":{"x":"date","y":"price","zoom":True}}` followed by `{"type":"stats","fields":["price","price_1y_low","price_1y_high"]}`. The stable popup chart id must mount through the shared chart controller so legend, smoothing, zoom, click, brush, links, and current targeted-filter state behave like inline charts. Do not use popup `stat_grid`. Require non-empty representative CUSIP overlap before delivery and place exact source/client fields under each dataset entry's `field_provenance`. Let me know if frictions.

---

Build an `nfp_release_popup` dashboard using this exact supplied Haver contract: `pull_haver_data(codes=["USEMPNFP@USECON","LR@USECON","AHE@USECON","USEMPPRI@USECON","AWH@USECON"], start="2024-01-01", name="labor_release", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` and `nfp_thousands:float64, unemployment_pct:float64, ahe_usd_per_hour:float64, private_payrolls_thousands:float64, avg_hours:float64` in that code order. Transform to `release_summary` with exactly five latest rows and columns `metric:string,value:float64,unit:string,as_of:string`, where metric values are `NFP`, `Unemployment`, `AHE`, `Private Payrolls`, `Avg Hours`; and `release_history` in long form with `date:datetime64[ns],metric:string,value:float64,unit:string`, retaining 24 months. Tab id `release_summary` contains table id `release_table`; its `row_click` chart section uses `dataset="release_history"`, `row_key="metric"`, `filter_field="metric"`, mapping `x=date,y=value`. Before shipping, inspect popup findings and prove all five metric tokens overlap; if the source cannot be transformed to that exact long shape, report that required reshape and do not claim success. Let me know if frictions.

---

Build an `nfp_birth_death` dashboard from exact supplied Haver codes `["USEMPNFP@USECON","LR@USECON","AHE@USECON","USEMPPRI@USECON","AWH@USECON","USBIRTHDEATH@USECON"]` via `pull_haver_data(..., start="2024-01-01", name="labor_release", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` plus `nfp_thousands,unemployment_pct,ahe_usd_per_hour,private_payrolls_thousands,avg_hours,birth_death_thousands` as `float64`. Transform the first five fields to the same five-row `release_summary` and 24-month long `release_history` contract, and emit `birth_death` columns `date:datetime64[ns],actual:float64,ma_3m:float64,ma_6m:float64,ma_12m:float64` using rolling means with windows 3/6/12 and matching `min_periods`. Tab id `release_summary` has table id `release_table` with an independent single-series popup history chart joined on metric. Tab id `birth_death` has inline chart id `birth_death_trend` with exactly four visible series and ordinary legend toggles. Do not request popup/inline legend mirroring: popup legend state is independent; preserve the information by keeping all moving-average series in the inline chart. Let me know if frictions.

---

Build a `curve_narrative_cards` dashboard with a single tab id `overview` and one 12-column row containing three width-4 `markdown` widgets: id `curve_thesis`, kind `thesis`, title `Thesis`, content `The curve is bull-steepening for the third session.`; id `curve_watch`, kind `watch`, title `Watch`, content `Levels to monitor: 4.10% 10Y, 50dma; 2s10s out of inversion.`; id `curve_risk`, kind `risk`, title `Risk`, content `Stop-out: a hot CPI print would invalidate.` These are user-authored analytical statements, so preserve the supplied wording and do not invent supporting values. This is a narrative-only build: persist `PULLS = {}`, use `datasets = {}`, and persist `TRANSFORMS = []`; use metadata methodology `## Authorship\n* User-supplied scenario framing; no live numerical dataset.` and tags `["rates","narrative"]`. Let me know if frictions.

---

Build a `bond_drilldown` dashboard from three exact user-supplied verified calls: `bonds=bond_client.get_screen(universe="USD_IG", limit=50, fields=["cusip","issuer","rating","spread_bp","ytm_pct","duration_yrs","price","mod_duration_yrs","dv01"])` → `bonds` with string `cusip,issuer,rating` and the six measures as `float64`; `bond_history=bond_client.get_history(cusips=bonds["cusip"].tolist(), start="-180D", fields=["date","cusip","price"])` → `bond_history` with `date:string,cusip:string,price:float64`; and `bond_events=bond_client.get_events(cusips=bonds["cusip"].tolist(), start="-180D", fields=["date","cusip","event","source"])` → `bond_events` with four `string` columns. Persist each using `save_artifact` under its stated name. Table id `bond_table` uses a `row_click.detail.sections` list in this exact order: `{"type":"stats","fields":["price","ytm_pct","mod_duration_yrs","dv01"]}`; `{"type":"markdown","template":"### {issuer}\n\nRating: **{rating}**"}`; `{"type":"chart","dataset":"bond_history","row_key":"cusip","filter_field":"cusip","mapping":{"x":"date","y":"price"}}`; `{"type":"table","dataset":"bond_events","row_key":"cusip","filter_field":"cusip","columns":["date","event","source"],"max_rows":12}`. Do not use popup `stat_grid`; prove CUSIP overlap for both detail datasets and add exact field provenance. Let me know if frictions.

---

Build a `rates_headline_monitor` dashboard from exact verified expressions `["sofrswp2y","sofrswp10y","move_index"]` and labels `["us_2y","us_10y","move"]` using `pull_plottool_data(..., start="2024-01-01", name="rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` plus three `float64` columns (`us_2y/us_10y` percent, MOVE index points). Transform sorted daily data to `rates_stats` with `date,us_2y,us_10y,move,spread_bp=(us_10y-us_2y)*100`; deltas `us_2y_delta_bp=diff(us_2y)*100`, `us_10y_delta_bp=diff(us_10y)*100`, `spread_delta_bp=diff(spread_bp)`, `move_delta=diff(move)`; and latest-row fields `range_today_bp=abs(spread_bp)`, `weekly_change_bp=diff(us_10y,5)*100`, `monthly_change_bp=diff(us_10y,21)*100`, `ytd_change_bp=(us_10y-last_observation_before_current_calendar_year)*100`, `high_1y_2y=max(last 252 us_2y)`, `low_1y_2y=min(last 252 us_2y)`, `percentile_1y_2y_pct=100*mean(last_252_us_2y<=current_us_2y)`, and `percentile_1y_10y_pct=100*mean(last_252_us_10y<=current_us_10y)`, all numeric outputs `float64`. Tab id `overview` opens with four width-3 KPIs: `kpi_2y` sources `rates_stats.latest.us_2y`, delta `rates_stats.latest.us_2y_delta_bp`, sparkline `rates_stats.us_2y`; `kpi_10y` uses the analogous 10Y fields; `kpi_2s10s` uses `spread_bp/spread_delta_bp`; `kpi_move` uses `move/move_delta`. Add width-12 stat-grid id `rates_stats_grid` with exactly eight items sourcing `rates_stats.latest.<each exact latest-row field above>`. Put these formulas in methodology and dataset-entry provenance. Let me know if frictions.

---

Build an `earnings_pivot` dashboard from this exact user-supplied verified call: `earnings_client.get_quarterly(universe="SP500", start="2021-Q1", fields=["ticker","sector","region","quarter","eps_growth_pct","revenue_growth_pct","margin_pct"])`, returning `ticker,sector,region,quarter:string` and the three measures as `float64`; persist via `save_artifact(..., name="earnings", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`. On tab id `pivot`, add width-12 pivot id `earnings_pivot` with `dataset_ref="earnings"`, `row_dim_columns=["sector","region","ticker"]`, `col_dim_columns=["quarter"]`, `value_columns=["eps_growth_pct","revenue_growth_pct","margin_pct"]`, `agg_options=["mean","median","sum"]`, `color_scale={"min":-50,"max":50,"palette":"gs_diverging"}`, and `show_totals=true`. Do not author `row_totals` or `column_totals`; the supported boolean renders paired row and column totals. Add exact client field names, percent units, and quarterly frequency under `datasets.earnings.field_provenance`. Let me know if frictions.
