Build a `us_10y_regimes` dashboard with tab id `overview` and chart id `us10y_history`. Treat this exact PlotTool expression as user-supplied and verified—do not replace or infer it: `pull_plottool_data(expressions=["ustsy10y"], labels=["us_10y_pct"], start="2010-01-01", name="rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`. It persists `rates.csv` with `date:string` and `us_10y_pct:float64`, daily, percent. Use a line mapping `x=date`, `y=us_10y_pct`, `y_title="Yield (%)"`; vlines at `2015-12-16`, `2020-03-11`, and `2022-03-16`; and an hline at `2.0` labelled `2% reference`. Put lineage at `datasets.rates.field_provenance.us_10y_pct = {"system":"plottool","symbol":"ustsy10y","display_name":"US 10Y Treasury yield","units":"percent","frequency":"daily","source_label":"GS Market Data"}`. Use metadata summary `{"title":"US 10Y regimes","body":"Daily US 10Y yield with selected policy and stress regime markers."}` and tags `["rates","treasury","regimes"]`. Let me know if frictions.

---

Build a `big_tech_capex` dashboard from exact verified call `pull_plottool_data(expressions=["AAPL_CAPEX_ACTUAL_Q","AAPL_CAPEX_ESTIMATE_Q","MSFT_CAPEX_ACTUAL_Q","MSFT_CAPEX_ESTIMATE_Q","GOOG_CAPEX_ACTUAL_Q","GOOG_CAPEX_ESTIMATE_Q","META_CAPEX_ACTUAL_Q","META_CAPEX_ESTIMATE_Q","AMZN_CAPEX_ACTUAL_Q","AMZN_CAPEX_ESTIMATE_Q"], labels=["aapl_actual_bn","aapl_estimate_bn","msft_actual_bn","msft_estimate_bn","goog_actual_bn","goog_estimate_bn","meta_actual_bn","meta_estimate_bn","amzn_actual_bn","amzn_estimate_bn"], start="2018-01-01", name="capex", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` plus those ten `float64` USD-bn fields. Set `column_map={"aapl_actual_bn":["AAPL","actual","AAPL_CAPEX_ACTUAL_Q"],"aapl_estimate_bn":["AAPL","estimate","AAPL_CAPEX_ESTIMATE_Q"],"msft_actual_bn":["MSFT","actual","MSFT_CAPEX_ACTUAL_Q"],"msft_estimate_bn":["MSFT","estimate","MSFT_CAPEX_ESTIMATE_Q"],"goog_actual_bn":["GOOG","actual","GOOG_CAPEX_ACTUAL_Q"],"goog_estimate_bn":["GOOG","estimate","GOOG_CAPEX_ESTIMATE_Q"],"meta_actual_bn":["META","actual","META_CAPEX_ACTUAL_Q"],"meta_estimate_bn":["META","estimate","META_CAPEX_ESTIMATE_Q"],"amzn_actual_bn":["AMZN","actual","AMZN_CAPEX_ACTUAL_Q"],"amzn_estimate_bn":["AMZN","estimate","AMZN_CAPEX_ESTIMATE_Q"]}` and `TRANSFORMS=[derive_capex_long]`. The transform parses/sorts `date`, melts exact `column_map` keys, maps each source field to its exact company/state tuple, and emits canonical `capex_long(date:datetime64[ns],company:string,state:string,capex_bn:float64,capex_yoy_pct:float64)`, with YoY `groupby(["company","state"]).capex_bn.pct_change(4)*100`. It then melts `capex_long` with `id_vars=["date","company","state"]`, `value_vars=["capex_bn","capex_yoy_pct"]`, `var_name="metric"`, `value_name="value"`, drops null `value`, and emits `capex_chart_long(date:datetime64[ns],company:string,state:string,metric:string,value:float64)`, where metric values are exactly `capex_bn,capex_yoy_pct`; project exact same-schema company subsets `capex_aapl_chart,capex_msft_chart,capex_goog_chart,capex_meta_chart,capex_amzn_chart`. Add five chart widgets `capex_aapl,capex_msft,capex_goog,capex_meta,capex_amzn`, each with `spec.dataset` set to its matching subset, at most three per row. Every panel uses `chart_type="multi_line"` and mapping `{"x":"date","y":"value","color":"metric","strokeDash":"state","strokeDashScale":{"domain":["actual","estimate"],"range":[[1,0],[8,3]]},"strokeDashLegend":true,"dual_axis_series":["capex_yoy_pct — actual","capex_yoy_pct — estimate"],"y_title":"Capex (USD bn)","y_title_right":"YoY (%)"}`. Pin the four emitted names `capex_bn — actual`, `capex_bn — estimate`, `capex_yoy_pct — actual`, `capex_yoy_pct — estimate`; the first two use the left axis and the last two the right. Under `capex.field_provenance`, each source field gets `{"system":"plottool","symbol":column_map[field][2],"units":"USD bn","frequency":"quarterly"}`. Under `capex_long.field_provenance`, use `date={"system":"computed","recipe":"pd.to_datetime(capex.date)","computed_from":["date"],"units":"date"}`, `company={"system":"computed","recipe":"column_map[source_field][0]","computed_from":list(column_map),"units":"category"}`, `state={"system":"computed","recipe":"column_map[source_field][1]","computed_from":list(column_map),"units":"category"}`, `capex_bn={"system":"computed","recipe":"melt column_map keys to capex_bn","computed_from":list(column_map),"units":"USD bn"}`, and `capex_yoy_pct={"system":"computed","recipe":"groupby(company,state).capex_bn.pct_change(4)*100","computed_from":["company","state","capex_bn"],"units":"percent"}`. Under `capex_chart_long.field_provenance`, copy the canonical `date,company,state` entries and use `metric={"system":"computed","recipe":"melt capex_long value_vars to metric","computed_from":["capex_bn","capex_yoy_pct"],"units":"category"}` and `value={"system":"computed","recipe":"melt capex_long value_vars to value","computed_from":["capex_bn","capex_yoy_pct"],"units":"mixed; see metric"}`; copy all five entries unchanged to every company subset. Let me know if frictions.

---

Build a `cross_asset_correlation` dashboard with chart id `corr_63d`. Use this exact verified pull without identifier discovery: `pull_plottool_data(expressions=["ustsy2y","ustsy5y","ustsy10y","ustsy30y","spx_index","dxy_index","wti_front","xau_usd"], labels=["us_2y","us_5y","us_10y","us_30y","spx","dxy","wti","gold"], start="2018-01-01", name="cross_asset", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` and eight `float64` columns in label order. Author one `correlation_matrix` with `columns` equal to those eight labels, `method="pearson"`, `transform="pct_change"`, `order_by="date"`, `window="63d"`, `window_options=["63d","126d","252d","all"]`, `min_periods=20`, `show_values=true`, and `value_decimals=2`. Put source lineage for every column at `datasets.cross_asset.field_provenance`, preserving each exact expression and its units (`percent` for yields, `index` for SPX/DXY, `usd` for WTI/gold). Use metadata summary `{"title":"Cross-asset correlation","body":"Rolling Pearson correlation of daily percent changes; initial window 63 observations."}` and tags `["cross-asset","correlation","risk"]`. Let me know if frictions.

---

Build a `regional_choropleth_contract` dashboard from a deterministic user-supplied fixture. Persist `regional_snapshot` from `pd.DataFrame([{"region_key":"A","value":10.0,"owner":"Rates"},{"region_key":"Beta","value":20.0,"owner":"Credit"},{"region_key":"Gamma","value":15.0,"owner":"Equities"}])` through one `PULLS={"regional_snapshot":pull_regional_snapshot}` function using `save_artifact(..., name="regional_snapshot", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`; keep `TRANSFORMS=[]`. Add `map_assets.mini_regions={"geojson":{"type":"FeatureCollection","features":[{"type":"Feature","properties":{"name":"Alpha"},"geometry":{"type":"Polygon","coordinates":[[[0,0],[1,0],[1,1],[0,1],[0,0]]]}},{"type":"Feature","properties":{"name":"Beta"},"geometry":{"type":"Polygon","coordinates":[[[2,0],[3,0],[3,1],[2,1],[2,0]]]}},{"type":"Feature","properties":{"name":"Gamma"},"geometry":{"type":"Polygon","coordinates":[[[4,0],[5,0],[5,1],[4,1],[4,0]]]}}]},"name_property":"name","aliases":{"A":"Alpha"}}`. Row 0 is hero chart id `regional_map`, width 12, with `chart_type="geo_map"` and mapping `{"region":"region_key","value":"value","map":"mini_regions","roam":True}`. Row 1 is full-width `data_grid` id `regional_grid` over the same dataset, columns `region_key,value,owner`, `page_size=20`, and `h_px=420`. Add non-overlapping groups `{"id":"map_section","title":"Map","start_row":0,"end_row":0}` and `{"id":"detail_section","title":"Details","start_row":1,"end_row":1,"collapsible":True}`. Add select filter id `region_filter`, field `region_key`, options `["","A","Beta","Gamma"]`, default `""`, all value `""`, and targets `["regional_map","regional_grid"]`. Put static-fixture provenance under every field. Strict-compile, verify alias `A→Alpha` and zero unknown/duplicate-region findings, then verify chart-data CSV and PDF/print actions are present; print must include every tab and open collapsed groups for capture. Let me know if frictions.

---

Build a `macro_multi_axis` dashboard with tab id `overview` and chart id `macro_levels`. Use this exact verified call: `pull_plottool_data(expressions=["spx_index","ustsy10y","dxy_index","wti_front"], labels=["spx","us_10y_pct","dxy","wti_usd"], start="2020-01-01", name="macro_levels", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` and four `float64` columns. Use `multi_line`, `x=date`, `y=["spx","us_10y_pct","dxy","wti_usd"]`, and exactly four axes: SPX left with title `S&P 500` and `format="compact"`; US 10Y right with title `UST 10Y (%)`, `format="percent"`, `invert=true`; DXY left with title `DXY`; WTI right with title `WTI ($)` and `format="usd"`, with offsets chosen to prevent label collision. Keep the four-series cap intact. Put exact expression, display name, units, and daily frequency per field under `datasets.macro_levels.field_provenance`. Let me know if frictions.

---

Build a `rates_rv_compute` dashboard from this exact verified pull: `pull_plottool_data(expressions=["sofrswp2y","sofrswp10y"], labels=["us_2y","us_10y"], start="2015-01-01", name="rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string`, `us_2y:float64`, and `us_10y:float64`, all rates in percent. In the template declare `datasets.rates={"source":[],"field_provenance":...}` and `datasets.rates_rv={"from":"rates","compute":{"us_2s10s_bp":"(us_10y - us_2y) * 100","us_10y_z_60":"zscore(us_10y, 60)"}}`; do not move these formulas to an unspecified helper. The compiler must emit `date`, `us_2s10s_bp`, and `us_10y_z_60` with computed lineage. Render a 2-up row on tab id `overview`: chart id `spread_2s10s` maps `x=date,y=us_2s10s_bp,y_title="2s10s (bp)"`; chart id `us10y_z60` maps `x=date,y=us_10y_z_60,y_title="60-observation z-score"`. Use metadata methodology text `## Sources\n* GS Market Data\n## Construction\n* 2s10s = (10Y - 2Y) * 100 bp\n* 10Y z-score uses rolling 60 observations with sample standard deviation.` and tags `["rates","relative-value","computed"]`. Let me know if frictions.

---

Build a `sector_month_heatmap` dashboard from this exact verified pull: `pull_plottool_data(expressions=["SPX_ENERGY_TR","SPX_MATERIALS_TR","SPX_INDUSTRIALS_TR","SPX_CONS_DISC_TR","SPX_CONS_STAPLES_TR","SPX_HEALTH_CARE_TR","SPX_FINANCIALS_TR","SPX_INFO_TECH_TR","SPX_COMM_SERVICES_TR","SPX_UTILITIES_TR","SPX_REAL_ESTATE_TR"], labels=["energy","materials","industrials","consumer_discretionary","consumer_staples","health_care","financials","information_technology","communication_services","utilities","real_estate"], start="2018-01-01", name="sector_tr", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` plus eleven total-return-index `float64` columns. Transform month-end levels with `resample("ME").last().pct_change()*100` and melt to `sector_month` columns `month:string`, `sector:string`, `return_pct:float64`. Chart id `sector_heatmap` maps `x=month,y=sector,value=return_pct`, `show_values=true`, `value_decimals=1`, `color_scale="diverging"`, `value_min=-15`, `value_max=15`, and `colors=["#67001F","#8A1538","#B2182B","#D6604D","#F4A582","#F7C6B6","#FDE0DD","#F7F7F7","#DDEBF7","#BDD7EE","#92C5DE","#67A9CF","#4393C3","#2166AC","#053061"]` with white at index 7. Add computed provenance for `return_pct` with the exact monthly formula and units `percent`. Let me know if frictions.

---

Build a `timeseries_integrity_guard` dashboard from this exact deterministic user fixture: daily `date` values from `2026-01-01` through `2026-01-15` and `value=[100,101,None,102,103,None,104,105,106,107,108,109,110,111,10000]`. One zero-argument `pull_series` function must preserve those observations and nulls exactly and persist dataset `series` with `save_artifact(frame, name="series", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)` through `PULLS={"series":pull_series}`; use `TRANSFORMS=[]`. Set both fields' provenance to `{"system":"fixture","source":"user_supplied","source_label":"User-supplied deterministic fixture"}`, adding units `date` and `index` respectively. Use metadata source `User-supplied deterministic fixture`, methodology `## Construction\n* Exact user-supplied daily fixture; no imputation or normalization.`, and `refresh_frequency="manual"`. Add line chart id `quality_line` mapping `x=date,y=value`. Declare `datasets.series.quality={"time_field":"date","expected_frequency":"1D","severity":"warning","fields":{"value":{"max_missing_fraction":0.20,"max_internal_na_run":1,"outlier":{"method":"mad","threshold":8,"max_scale_expansion":4}}}}`. Strict-compile, then inspect and report every `data_quality_*` and `timeseries_*` warning with its structured evidence and `visual_effect`. Preserve the source observations exactly: do not sort, fill, clip, winsorize, delete, or otherwise repair an ambiguous observation. If the `10000` point may be genuine, ask before changing it. Let me know if frictions.

---

Build a `named_series_colors` dashboard from the exact deterministic user fixture `pd.DataFrame([{"date":"2026-01-01","series":"Nominal","value":4.0},{"date":"2026-01-01","series":"Real","value":1.7},{"date":"2026-01-02","series":"Nominal","value":4.1},{"date":"2026-01-02","series":"Real","value":1.8}])`, persisted as `rates` through a zero-argument fixture pull with durable imports and `save_artifact`; use `TRANSFORMS=[]`, fixture provenance, and manual refresh. Add chart id `rates_compare` with `chart_type="multi_line"`, mapping `x=date,y=value,color=series`, and exact `spec.series_colors={"Nominal":{"light":"#002F6C","dark":"#74C0E3"},"Real":{"light":"#8A1538","dark":"#FF8A80"}}`. Verify Nominal and Real receive those exact named colors in both viewer modes rather than relying on row order or palette-slot guessing. Let me know if frictions.
