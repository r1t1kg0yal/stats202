Inspect inherited dashboard `rates_monitor`, whose required files exist, canonical first tab is `overview`, dataset `rates` is persisted at `data/rates.csv` with `date:string,us_2y_pct:float64,us_10y_pct:float64`, and existing widget ids are `curve_history,kpi_2y,kpi_10y`. Treat current inspection as authoritative. Record inspection state, canonical tab order, complete widget/filter/link/dataset ids, and bytes. Use `apply_manifest_operations` with `expected_sha256=state["manifest_template_sha256"]` and one `add_widget` operation to append `{"widget":"chart","id":"us10y_histogram","w":6,"title":"US 10Y yield distribution","spec":{"chart_type":"histogram","dataset":"rates","mapping":{"x":"us_10y_pct"}}}` to the bottom of Overview. Require changed `post_sha256`, preserve every pre-existing id/definition and dataset column, rebuild/refresh, and verify placement/consumer/schema through inspection. Let me know if frictions.

---

Inspect inherited dashboard `cross_asset_monitor` with canonical tabs `overview,risk`, widget id `risk_old` currently at Risk row 2 index 1, filter id `risk_window` targets `["risk_old"]`, and link group `risk_zoom` members `["risk_old","volatility"]`. Record current SHA and definitions. Apply one ordered typed transaction: `update_widget` selector `{"id":"risk_old"}` with patch `{"id":"risk_current","title":"Risk regime"}`, then `move_widget` selector `{"id":"risk_current"}` with destination `{"tab_id":"risk","row":1,"index":0}`. Verify post-SHA differs; old id is absent; `risk_window.targets=["risk_current"]`; `risk_zoom.members=["risk_current","volatility"]`; canonical tabs and every unrelated widget remain unchanged; then rebuild/refresh. Let me know if frictions.

---

Extend inherited `cross_asset_monitor`, whose canonical tabs are `overview,fx` and whose current datasets do not include credit. Before any manifest operation, pre-provision data through pipelines with exact supplied call `bond_client.get_screen(universe="USD_IG_HY", fields=["cusip","issuer","rating_bucket","spread_bp","ytm_pct"])`, returning `cusip,issuer,rating_bucket:string,spread_bp:float64,ytm_pct:float64`; persist as `credit`, add the PULLS entry, run it, and prove `datasets.credit` plus field provenance exists. Then inspect current template SHA and apply one typed transaction: `add_tab` id `credit`, label `Credit`, at index 1; add width-6 scatter id `credit_spreads` mapping `x=spread_bp,y=ytm_pct,color=rating_bucket`; add width-6 table id `credit_bonds` with issuer/rating/spread/YTM; add multiSelect filter id `credit_rating` with bare field `rating_bucket`, options `["AAA","AA","A","BBB","BB","B","CCC"]`, default all, and exact targets `["credit_spreads","credit_bonds"]`. Do not replace the layout root. Verify tabs `overview,credit,fx`, data attachment, and preserved unrelated state. Let me know if frictions.

---

Inspect inherited dashboard `rates_migration` where dataset `obsolete_rates` is expected to feed widget ids `obsolete_curve,obsolete_kpi`, filter id `obsolete_tenor` through both field/options dependency, and link group `obsolete_zoom` with members `["obsolete_curve","current_curve"]`; current dataset `rates` and widget `current_curve` must survive. Do not assume the expected graph is complete: use the inspection graph to enumerate every actual widget, filter target/options dependency, link member, producer, and persisted path tied to `obsolete_rates`, and report the complete destructive impact set before mutation. With explicit user intent to remove every obsolete-rates surface, apply typed `{"op":"remove_dataset","name":"obsolete_rates"}` at the current SHA; its cascade is built in and takes no cascade key. Verify the returned removals match the inspected impact, post-SHA differs, no dangling obsolete reference remains, current rates/current_curve survive unchanged, then strict build/refresh and report the visible remaining layout. Let me know if frictions.

---

Inspect inherited dashboard `daily_macro_monitor`; require exactly one registry match and record its `created_at`, canonical entry, template SHA, and both current refresh frequencies `1d`. Apply one typed `patch_metadata` operation with exact patch `{"summary":{"title":"Daily macro monitor","body":"Hourly refreshed rates, inflation, and labor-market snapshot."},"tags":["macro","rates","inflation","labor"],"refresh_frequency":"1h"}`; the operation's atomic cadence synchronization must update the one matching registry entry. Do not rename the dashboard root. Verify one registry match remains, `created_at` is byte-for-byte unchanged, template and registry both say `1h`, manifest pointer state is reported, post-SHA differs, and summary/tags equal the supplied content; then rebuild/refresh. Let me know if frictions.

---

Use inspection state A from inherited `rates_monitor` to prepare intended widget patch for id `curve_history`: change only title from `Rates curve` to `US swap curve history`, preserving its complete current definition. Record SHA A. Then model a concurrent author change before the write: state B changes metadata tags from `["rates"]` to exact `["rates","macro"]`, producing SHA B. Attempt the intended `update_widget` with stale `expected_sha256=SHA A` and require a concurrency rejection with no mutation. Inspect state B, prove the tag change, rebase the one-field title patch onto B's complete widget, retry with `expected_sha256=SHA B`, and verify final tags remain `["rates","macro"]`, title is updated, all other widget bytes are preserved, and returned pre/post hashes match B/final bytes. Let me know if frictions.

---

Inspect inherited `rates_monitor` and retain exact prior bytes/SHA for template, registry, compiled manifest, and HTML. Submit one two-operation transaction at the current SHA: first `update_widget` changes KPI id `kpi_10y` label from `10Y` to `US 10Y`; second `update_widget` changes chart id `curve_history` mapping from valid `{"x":"date","y":["us_2y_pct","us_10y_pct"]}` to invalid `{"x":"missing_date","y":["us_2y_pct","us_10y_pct"]}`. Require validation/recompile failure and an exception stating rollback completed; treat the operation list as uncommitted. Prove rollback with a fresh inspection: template SHA equals the retained pre-SHA, and template, registry, compiled manifest, and HTML match retained bytes exactly. Then retry both operations with corrected chart mapping `{"x":"date","y":["us_2y_pct","us_10y_pct"]}` and that fresh SHA; on success verify serialized `pre_sha256` equals expected SHA, `rollback_sha256` equals pre-SHA, `post_sha256` equals the final inspected SHA, both intended updates committed, and build/refresh succeed. Let me know if frictions.
