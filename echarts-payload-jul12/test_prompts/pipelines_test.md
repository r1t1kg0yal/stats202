Build a `three_source_macro` dashboard with these exact supplied pull bodies and `PULLS={"rates":pull_rates,"cpi":pull_cpi,"labor":pull_labor}`: `pull_plottool_data(expressions=["sofrswp2y","sofrswp10y"], labels=["us_2y_pct","us_10y_pct"], start="2015-01-01", name="rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)` → `date:string` plus two `float64` percent fields; `pull_haver_data(codes=["JCXFE@USECON","PCU@USECON"], start="2015-01-01", name="cpi", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)` → `date:string,core_pce_yoy_pct:float64,headline_cpi_yoy_pct:float64`; `pull_fred_data(series=["UNRATE"], start="2015-01-01", name="labor", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)` → `date:string,unemployment_rate_pct:float64`. Keep `TRANSFORMS=[]`. Across tab ids `rates` and `macro`, create exactly six consumers: chart `rates_history`; KPIs `kpi_2y` and `kpi_10y`; chart `inflation_history`; chart `unemployment_history`; and stat_grid `latest_inflation` with stats `[{"id":"core_pce","label":"Core PCE YoY","source":"cpi.latest.core_pce_yoy_pct","suffix":"%","decimals":2},{"id":"headline_cpi","label":"Headline CPI YoY","source":"cpi.latest.headline_cpi_yoy_pct","suffix":"%","decimals":2}]`. Add source field provenance under each dataset. Once the build is live, call `inspect_dashboard` and report exact `PULLS key → CSV stem → dataset key → consumer ids` edges plus the three PULLS keys and empty TRANSFORMS. Let me know if frictions.

---

Build a `rates_distribution_edit` dashboard with `PULLS={"rates":pull_rates}` where `pull_rates` calls exactly `pull_plottool_data(expressions=["sofrswp2y","sofrswp5y","sofrswp10y","sofrswp30y"], labels=["us_2y_pct","us_5y_pct","us_10y_pct","us_30y_pct"], start="2015-01-01", name="rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` plus four `float64` percent fields. Set `TRANSFORMS=[]`. Feed one four-line chart id `curve_history` and KPI ids `kpi_2y,kpi_5y,kpi_10y,kpi_30y`. Then do not edit `pull_data.py`: inspect current template SHA and dataset columns, add histogram id `us10y_distribution` over existing `rates.us_10y_pct` to the bottom row through one typed `add_widget` transaction, rebuild/refresh, and verify the new consumer while preserving every existing field and widget. Let me know if frictions.

---

Build a `rates_extend_existing_pull` dashboard with `PULLS={"rates":pull_rates}` and initial exact call `pull_plottool_data(expressions=["sofrswp2y","sofrswp10y"], labels=["us_2y_pct","us_10y_pct"], start="2015-01-01", name="rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` and two `float64` percent fields. Set `TRANSFORMS=[]` and add chart ids `us2y_history` and `us10y_history`. Then add 5Y by surgically changing only the existing lists to `expressions=["sofrswp2y","sofrswp5y","sofrswp10y"]` and `labels=["us_2y_pct","us_5y_pct","us_10y_pct"]`; require one unique edit anchor and changed script bytes. Run `run_pull(folder,"rates")`, prove `rates.csv` now contains `us_5y_pct:float64` while preserving the other columns, then add chart id `us5y_history` without creating another PULLS entry. Let me know if frictions.

---

Build a `rates_with_gs_bank` dashboard with initial `PULLS={"rates":pull_rates}` using exact verified rates call `pull_plottool_data(expressions=["sofrswp2y","sofrswp10y"], labels=["us_2y_pct","us_10y_pct"], start="2015-01-01", name="rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)` → `date:string,us_2y_pct:float64,us_10y_pct:float64`, and two rates charts. Then add genuinely new `pull_gs_bank` using exact user-supplied client form `payload=fdic_client.get("financials", params={"filters":"CERT:33124","fields":"REPDTE,ASSET,DEP,NIMY,ROA","limit":8,"sort_by":"REPDTE","sort_order":"DESC"})`; construct a DataFrame from `payload["data"]`, rename to `report_date,assets_usd_thousands,deposits_usd_thousands,net_interest_margin_pct,return_on_assets_pct`, cast date to string and measures to float64, sort ascending, and call `save_artifact(df, name="gs_bank", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`. Add `PULLS["gs_bank"]=pull_gs_bank`, keep `TRANSFORMS=[]`, run only that pull, verify exactly eight quarters, and render assets/deposits in explicit two-chart small multiples plus one two-series NIM/ROA chart. Let me know if frictions.

---

Build a `rates_dependency_removal` dashboard with `PULLS={"rates":pull_rates}` using exact call `pull_plottool_data(expressions=["sofrswp2y","sofrswp5y","sofrswp10y"], labels=["us_2y_pct","us_5y_pct","us_10y_pct"], start="2015-01-01", name="rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)` → `date:string` plus three `float64` percent fields. Set `TRANSFORMS=[derive_curve_long,derive_spread]`: `derive_curve_long` parses/sorts `date`, melts exact value vars `["us_2y_pct","us_5y_pct","us_10y_pct"]`, maps them to tenor labels `["2Y","5Y","10Y"]`, and emits `curve_long(date:datetime64[ns],tenor:string,yield_pct:float64)`; `derive_spread` emits `spread(date:datetime64[ns],spread_2s10s_bp:float64)` with `(us_10y_pct-us_2y_pct)*100`. Chart `curve_history` maps `curve_long` as `x=date,y=yield_pct,color=tenor`; table `curve_table` consumes `curve_long`; select `tenor_filter` has bare field `tenor`, options `["2Y","5Y","10Y"]`, default `10Y`, targets `["curve_history","curve_table"]`. Chart `spread_2s10s` consumes `spread`; KPI ids `kpi_2y,kpi_10y` consume `rates`. Then remove 2Y entirely. Before applying the destructive change, call `inspect_dashboard` and enumerate the 2Y pull expression/label, `derive_curve_long` value-var/label, `derive_spread`, `rates/curve_long/spread` columns, `curve_history,curve_table,spread_2s10s,kpi_2y`, and filter option `2Y`. Remove the pull token, 2Y long mapping, 2s10s transform/dataset/chart, KPI `kpi_2y`, and option `2Y`; preserve 5Y/10Y curve surfaces and `kpi_10y`. Run the affected pull, strict build/refresh, and prove no stale 2Y reference remains. Let me know if frictions.

---

Build a `rates_macro_script_edit` dashboard with initial `PULLS={"rates":pull_rates}` using exact call `pull_plottool_data(expressions=["sofrswp2y","sofrswp10y"], labels=["us_2y_pct","us_10y_pct"], start="2015-01-01", name="rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)` → `date:string` plus two `float64` fields, `TRANSFORMS=[]`, and chart ids `us2y_history,us10y_history`. Then read current `pull_data.py` bytes and make one unique-anchor surgical edit that adds `pull_macro`, whose exact body calls `pull_haver_data(codes=["LR@USECON","JCXFE@USECON"], start="2015-01-01", name="macro", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)` and produces `date:string,unemployment_rate_pct:float64,core_pce_yoy_pct:float64`, plus `PULLS["macro"]=pull_macro`. Require changed script bytes and preserved unrelated content, run only macro, verify schema, then add two-series chart id `macro_history` consuming the new dataset and verify changed compiled output. Let me know if frictions.

---

Build a `pipeline_edge_audit` dashboard with initial exact `pull_rates` call `pull_plottool_data(expressions=["sofrswp2y","sofrswp10y"], labels=["us_2y_pct","us_10y_pct"], start="2015-01-01", name="rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)` → `rates.csv` columns `date:string,us_2y_pct:float64,us_10y_pct:float64`; `PULLS={"rates":pull_rates}`; `TRANSFORMS=[]`; and charts `us2y_history,us10y_history`. Add `pull_macro` using exact Haver call `pull_haver_data(codes=["LR@USECON","JCXFE@USECON"], start="2015-01-01", name="macro", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)` → `macro.csv` columns `date:string,unemployment_rate_pct:float64,core_pce_yoy_pct:float64`, add it under PULLS key `macro`, and add chart `macro_history`. After the edit, call `inspect_dashboard` and report exact edges for both keys/stems/datasets/three consumers, dtypes, producer classifications, and preserved rates columns/empty TRANSFORMS; then require strict build and clean refresh. Let me know if frictions.
