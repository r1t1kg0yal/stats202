Build a `global_cross_asset_filters` dashboard from exact verified expressions `["SPX_US_TR","STOXX_EU_TR","TOPIX_JP_TR","FTSE_UK_TR"]` labelled `["US","EU","JP","UK"]` via `pull_plottool_data(..., start="2010-01-01", name="regional_assets", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` plus four total-return-index `float64` columns. Transform to long-form `performance` columns `date:datetime64[ns],region:string,indexed_return_pct:float64,daily_return_pct:float64`; chart id `regional_performance` and table id `regional_snapshot` both consume it. Add top-bar dateRange id `window` with bare field `date`, exact targets `["regional_performance","regional_snapshot"]`, canonical default `"MAX"`, and the engine's fixed visible presets `1M / 3M / 6M / YTD / 1Y / 2Y / 5Y / All`; do not author an options key. Add multiSelect id `region` with bare field `region`, options/default `["US","EU","JP","UK"]`, and the same exact targets. Under `regional_assets.field_provenance`, map each returned label to `{"system":"plottool","symbol":<its exact expression>}`; under `performance`, mark indexed/daily returns computed with exact recipes/upstream columns. Let me know if frictions.

---

Build a `fundamental_cascade` dashboard from exact supplied call `equity_client.get_fundamentals(universe="GLOBAL_LARGE_CAP", fields=["ticker","company","region","country","sector","market_cap_usd","fwd_pe"])`, returning the first five fields as `string` and the measures as `float64`; its verified test rows include `AAPL/US/US`, `MSFT/US/US`, `SAP/EU/DE`, and `ASML/EU/NL`, and it persists as `fundamentals`. Add select filters in this exact declaration/dependency order, each with exact targets `["fundamental_table","valuation_scatter"]`: `region_filter` with bare field `region`, options `["US","EU"]`, default `US`; `country_filter` with bare field `country`, initial options `["US"]`, default `US`, `depends_on="region_filter"`, and `options_from={"dataset":"fundamentals","key":"country","where":"region == ${region_filter}"}`; `ticker_filter` with bare field `ticker`, initial options `["AAPL","MSFT"]`, default `AAPL`, `depends_on="country_filter"`, and `options_from={"dataset":"fundamentals","key":"ticker","where":"country == ${country_filter}"}`. Rebuilt options use engine lexical sorting: switching US→EU resets country to `DE`, then ticker to `SAP`. For every returned field `<field>`, set exact provenance `{"system":"client","client":"equity_client","method":"get_fundamentals","identifier":"<field>"}`, where the identifier is the exact caller-supplied field token. Let me know if frictions.

---

Build a `rates_synced_zoom` dashboard from exact verified expressions `["sofrswp2y","sofrswp5y","sofrswp10y","sofrswp30y","move_index"]` labelled `["us_2y","us_5y","us_10y","us_30y","move"]` via `pull_plottool_data(..., start="2015-01-01", name="rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` plus five `float64` columns. Transform `curve_change_bp` as each tenor's daily diff times 100 and `spread_2s10s_bp=(us_10y-us_2y)*100`. On tab id `overview`, add chart ids `curve_level`, `curve_change`, `spread_2s10s`, and `move_index` in a 2x2 layout; each time-axis chart explicitly sets `spec.chart_zoom=true`, which enables inside plus slider zoom. Add one link `{"group":"rates_zoom","members":["curve_level","curve_change","spread_2s10s","move_index"],"sync":["dataZoom"]}` to synchronize those existing zoom windows; do not treat the link as enabling zoom. Verify member ids and canonical tab order with inspection. Add exact source and computed provenance. Let me know if frictions.

---

Build a `sector_exposure_click_filter` dashboard from exact supplied call `equity_client.get_fundamentals(universe="SP500", fields=["ticker","company","sector","market_cap_usd","weight_pct","fwd_pe"])`, returning `ticker,company,sector:string` and the three measures as `float64`; persist as `fundamentals`. Transform `sector_weights` to `sector:string,market_cap_usd:float64,weight_pct:float64` by group sum. Add select filter id `sector_filter` with bare field `sector`, sorted unique returned sector options, default/all_value `""`, visible label `All`, and exact targets `["screener_table"]`. Donut id `sector_donut` maps `category=sector,value=weight_pct` and declares `click_emit_filter={"filter_id":"sector_filter","value_from":"name","toggle":true}`; clicking a slice filters the table and re-clicking clears to the empty all_value. For every returned field `<field>`, use `{"system":"client","client":"equity_client","method":"get_fundamentals","identifier":"<field>"}` with the exact caller token; mark grouped sector fields computed with exact recipe/upstreams. Let me know if frictions.

---

Build a `bond_compound_rule` dashboard from exact supplied call `bond_client.get_screen(universe="USD_CORPORATES", fields=["cusip","issuer","rating_bucket","sector","ytm_pct","spread_bp","duration_yrs"])`, returning the first four fields as `string` and three measures as `float64`; persist as `bonds`. Add rule filter id `bond_rule` with exact targets `["bond_table","spread_yield"]`, `rule={"all":[{"field":"rating_bucket","op":"in","value":["AAA","AA","A","BBB"]},{"any":[{"all":[{"field":"ytm_pct","op":">","value":4.5},{"field":"spread_bp","op":"<","value":200}]},{"all":[{"field":"sector","op":"in","value":["Financials","Banks"]},{"field":"duration_yrs","op":"<","value":5}]}]}]}`, and summary string `Investment grade and either YTM >4.5% with spread <200bp, or Financials/Banks duration <5y`. Rely on automatic summary rendering; do not author chip, subtitle, or popup UI keys. Preserve clause evidence with rating/sector/YTM/spread/duration table columns. For every returned field `<field>`, use `{"system":"client","client":"bond_client","method":"get_screen","identifier":"<field>"}` with the exact caller token. Let me know if frictions.

---

Build a `macro_tab_filters` dashboard from exact verified expressions `["sofrswp2y","sofrswp5y","sofrswp10y","eurusd","usdjpy","gbpusd","ig_oas","hy_oas"]` labelled `["US2Y","US5Y","US10Y","EURUSD","USDJPY","GBPUSD","IG","HY"]` via `pull_plottool_data(..., start="2015-01-01", name="macro", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` plus eight `float64` columns. Transform to long datasets `rates(date,tenor,value)`, `fx(date,pair,value)`, and `credit(date,rating,value)`. Add tabs in canonical order `us_rates`, `fx`, `credit`, with chart ids `rates_history`, `fx_history`, `credit_history`. Add tab-inline filters: select id `tenor`, bare field `tenor`, options `["US2Y","US5Y","US10Y"]`, default `US10Y`, targets `["rates_history"]`; multiSelect id `pair`, bare field `pair`, options/default `["EURUSD","USDJPY","GBPUSD"]`, targets `["fx_history"]`; multiSelect id `rating`, bare field `rating`, options/default `["IG","HY"]`, targets `["credit_history"]`. Add top-bar dateRange id `window`, bare field `date`, canonical default `MAX`, fixed visible presets `1M / 3M / 6M / YTD / 1Y / 2Y / 5Y / All`, no options key, and targets `["rates_history","fx_history","credit_history"]`. Let me know if frictions.

---

Build an `fx_vol_regime_visibility` dashboard from exact verified expressions `["vix_index","eurusd_1m_vol","usdjpy_1m_vol","gbpusd_1m_vol"]` labelled `["vix","EURUSD","USDJPY","GBPUSD"]` via `pull_plottool_data(..., start="2020-01-01", name="fx_vol", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` plus four `float64` fields. Transform `regime_latest` to one row `vix:float64` and `fx_vol_long` to `date:datetime64[ns],scope:string,pair:string,vol_pct:float64`, assigning scope `global` to all three pairs for this test. Add top-level select filter id `scope` with bare field `scope`, options `["global"]`, default `global`, and exact targets `["global_vol_pivot"]`. Risk markdown id `high_vix_warning` uses compile-time scalar condition `show_when={"data":"regime_latest.latest.vix","op":">","value":25}`. Pivot id `global_vol_pivot` uses distinct runtime condition `show_when={"filter":"scope","value":"global"}`. Do not substitute row-existence grammar for either condition. Under `fx_vol.field_provenance`, map each returned label to `{"system":"plottool","symbol":<its exact expression>}` and add exact transform recipes/upstreams to derived fields. Let me know if frictions.
