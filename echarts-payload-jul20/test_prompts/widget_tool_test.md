Before publishing, set `review = review_dashboard(FOLDER)`, inspect its one-line-per-panel receipt and every flagged `review.panel(panel_id)`, call `acknowledge_dashboard_review(FOLDER, expected_review_signature=review.review_signature, rationale="<specific acceptance rationale>")`, then call `build_dashboard(FOLDER, expected_current_version_id=... when the existing-version guard requires it)`; repair and re-review `BLOCK` instead of acknowledging it.
Build a tool-only `bond_ytm_solver` dashboard with `PULLS = {}`, `datasets = {}`, and `TRANSFORMS = []`. Tool id `bond_solver` has scalar inputs `face` default 100 min 0.01; `coupon_pct` default 4.5 min 0; `freq` select values `[1,2,4,12]` default 2; `settlement` date default `2026-07-10`; `maturity` date default `2031-07-10`; radio `input_mode` values `["price","yield"]` default `price`; `price` default 98; `ytm_pct` default 5. Inputs are annual percent where named pct. Set `T=(maturity-settlement)/365.25`, `N=max(1,round(T*freq))`, coupon `c=face*coupon_pct/100/freq`, final cashflow `c+face`, and price `sum(CF_i/(1+ytm/freq)^i)`. In price mode solve decimal YTM by bounded Newton iteration: 80 iterations, absolute price tolerance `1e-10`, bounds `[-0.5,5]`; reject nonconvergence. In yield mode calculate price directly. Declare `{"id":"headline","kind":"stat_grid","label":"Valuation"}` and return exactly `headline=[{"id":"price","label":"Price","value":<price>,"format":"number","decimals":2},{"id":"ytm_pct","label":"YTM","value":<100*decimal_ytm>,"format":"percent","decimals":3}]`. Declare cashflow schedule table `cashflows` with exact columns `[{"field":"period","label":"Period","format":"integer"},{"field":"yrs","label":"Years","format":"number:4"},{"field":"cashflow","label":"Cashflow","format":"number:2"},{"field":"df","label":"Discount factor","format":"number:8"},{"field":"pv","label":"Present value","format":"number:2"}]` and return one row per period. Return only `headline` and `cashflows`; do not over-declare duration, convexity, or DV01. Keep literal `compute_js` only in initial `tool_def`. Let me know if frictions.

---

Before publishing, set `review = review_dashboard(FOLDER)`, inspect its one-line-per-panel receipt and every flagged `review.panel(panel_id)`, call `acknowledge_dashboard_review(FOLDER, expected_review_signature=review.review_signature, rationale="<specific acceptance rationale>")`, then call `build_dashboard(FOLDER, expected_current_version_id=... when the existing-version guard requires it)`; repair and re-review `BLOCK` instead of acknowledging it.
Build a `fed_scenario_dashboard` with exact verified pull `pull_plottool_data(expressions=["sofrswp1y","sofrswp2y","sofrswp3y","sofrswp4y","sofrswp5y","sofrfwd1y1y","sofrfwd2y1y","sofrfwd3y1y","sofrfwd4y1y","sofrfwd5y1y"], labels=["spot_1y","spot_2y","spot_3y","spot_4y","spot_5y","fwd_1y","fwd_2y","fwd_3y","fwd_4y","fwd_5y"], start="2020-01-01", name="swap_rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` plus ten `float64` percent fields; set `PULLS={"swap_rates":pull_swap_rates}` and `TRANSFORMS=[derive_spot_forward_diff]`. Before the manifest-only second phase, `derive_spot_forward_diff` must pre-provision dataset `spot_forward_diff` with latest-row columns `tenor:string,diff_bp:float64` for tenors `["1Y","2Y","3Y","4Y","5Y"]`, formula `(spot_ny-fwd_ny)*100`. Initial canonical tabs are `scenario_builder` then `visualization`. Scenario Builder contains tool id `fed_scenario_inputs`: scalar `starting_effr_pct=4.33`; matrix `cumulative_bp` with rows `[{"key":"2026-07-29","label":"Jul 2026"},{"key":"2026-09-16","label":"Sep 2026"},{"key":"2026-10-28","label":"Oct 2026"},{"key":"2026-12-09","label":"Dec 2026"},{"key":"2027-01-27","label":"Jan 2027"}]`, columns `[{"id":"base","label":"Base"},{"id":"hawk","label":"Hawk"},{"id":"dove","label":"Dove"}]`, number-cell default 0 and step 25; stat_grid output `implied_swap_rates` where each scenario/tenor value is `starting_effr_pct + terminal_cumulative_bp/100 + {1:0.05,2:0.10,3:0.15,4:0.20,5:0.25}[tenor]`. Visualization contains five charts ids `swap_1y,swap_2y,swap_3y,swap_4y,swap_5y`; each maps date plus only its matching spot/forward pair, so every chart has two series. Then add third tab id `spot_forward_diff`, label `Spot vs Forward`, at index 2 plus bar chart id `spot_forward_diff_chart` mapping `x=tenor,y=diff_bp` through typed manifest operations using the inspected current SHA. Do not create data in the manifest phase. Verify exact equality of the Scenario Builder widget/tool_def and all five existing chart definitions before/after. Let me know if frictions.

---

Before publishing, set `review = review_dashboard(FOLDER)`, inspect its one-line-per-panel receipt and every flagged `review.panel(panel_id)`, call `acknowledge_dashboard_review(FOLDER, expected_review_signature=review.review_signature, rationale="<specific acceptance rationale>")`, then call `build_dashboard(FOLDER, expected_current_version_id=... when the existing-version guard requires it)`; repair and re-review `BLOCK` instead of acknowledging it.
Build a tool-only `fed_scenario_analysis` dashboard with `PULLS = {}`, `datasets = {}`, and `TRANSFORMS = []`. On tab `scenario_builder`, place tool id `fed_scenario_tool` at row 0, then adjacent visible markdown widget id `paste_example` at row 1 with exact text `Paste example (rows Jul 2026 through Jan 2027; columns Base, Hawk, Dove):\n-25\t0\t-25\n-50\t-25\t-75\n-50\t-25\t-100\n-75\t-50\t-125\n-75\t-50\t-150`. Tool inputs are scalar `starting_effr_pct=4.33`, date `trade_date=2026-07-10`, and matrix `cumulative_bp` with static rows `[{"key":"2026-07-29","label":"Jul 2026"},{"key":"2026-09-16","label":"Sep 2026"},{"key":"2026-10-28","label":"Oct 2026"},{"key":"2026-12-09","label":"Dec 2026"},{"key":"2027-01-27","label":"Jan 2027"}]`, columns `[{"id":"base","label":"Base"},{"id":"hawk","label":"Hawk"},{"id":"dove","label":"Dove"}]`, cell `{"type":"number","default":0,"step":25,"suffix":" bp"}`, and `paste_enabled=true`. In compute, its value is exactly `{rows,cols,values}` with row-major `values[r][c]`; reject non-finite paths and do not claim per-cell defaults. Meeting rate is `starting_effr_pct+cumulative_bp/100`; series `fed_funds_path` maps `x=date,y=rate_pct,color=scenario,x_format=date,y_format=percent` and returns 15 rows. Stat_grid `spot_rates` returns 15 items `<scenario>_<n>y`, with `spot_ny_pct=terminal_scenario_rate+{1:0.05,2:0.10,3:0.15,4:0.20,5:0.25}[n]`. Keep literal compute_js only inline. Let me know if frictions.

---

Before publishing, set `review = review_dashboard(FOLDER)`, inspect its one-line-per-panel receipt and every flagged `review.panel(panel_id)`, call `acknowledge_dashboard_review(FOLDER, expected_review_signature=review.review_signature, rationale="<specific acceptance rationale>")`, then call `build_dashboard(FOLDER, expected_current_version_id=... when the existing-version guard requires it)`; repair and re-review `BLOCK` instead of acknowledging it.
Build a tool-only `fed_taylor_rule` dashboard with `PULLS = {}`, `datasets = {}`, and `TRANSFORMS = []`. Tool id `taylor_rule_tool` has six numeric scalar inputs in percent/coefficients: `inflation_pct=2.7`, `target_inflation_pct=2.0`, `neutral_real_rate_pct=0.5`, `output_gap_pct=0.0`, `phi_pi=1.5`, `phi_y=0.5`. Literal `compute_js` returns stat id `implied_policy_rate_pct` using `neutral_real_rate_pct + inflation_pct + phi_pi*(inflation_pct-target_inflation_pct) + phi_y*output_gap_pct`; output format is percent with two decimals. Then raise only `tool_def.inputs[phi_pi].default` from 1.5 to 1.7 through a typed widget update that supplies the complete current tool_def at the inspected SHA. Verify compute_js exact equality and formula still reads runtime `v.phi_pi`, not a literal coefficient. Let me know if frictions.

---

Before publishing, set `review = review_dashboard(FOLDER)`, inspect its one-line-per-panel receipt and every flagged `review.panel(panel_id)`, call `acknowledge_dashboard_review(FOLDER, expected_review_signature=review.review_signature, rationale="<specific acceptance rationale>")`, then call `build_dashboard(FOLDER, expected_current_version_id=... when the existing-version guard requires it)`; repair and re-review `BLOCK` instead of acknowledging it.
Build a self-contained tool-only `bsm_option_pricer` dashboard with `PULLS = {}`, `datasets = {}`, and `TRANSFORMS = []`; do not reference an external example. Tool id `bsm_pricer` inputs are radio `kind` values `["call","put"]` default `call`; positive scalars `spot=100`, `strike=100`, `tte=1` year; and percent scalars `vol=20`, `rate=4.5`, `div=0`. Convert percent inputs to decimals. Define exactly `function normCDF(x){var ax=Math.abs(x);var t=1/(1+0.2316419*ax);var pdf=0.3989422804014327*Math.exp(-0.5*ax*ax);var upper=1-pdf*t*(0.319381530+t*(-0.356563782+t*(1.781477937+t*(-1.821255978+t*1.330274429))));return x>=0?upper:1-upper;}`. Use continuous-dividend BSM: `d1=(ln(S/K)+(r-q+0.5*sigma^2)*T)/(sigma*sqrt(T))`, `d2=d1-sigma*sqrt(T)`; call `S*exp(-qT)N(d1)-K*exp(-rT)N(d2)`; put `K*exp(-rT)N(-d2)-S*exp(-qT)N(-d1)`. Return stat `price`; stat_grid `greeks` with delta, gamma, vega raw/100, annual theta/365, and rho raw/100. Output the payoff curve as a series chart: declaration `payoff` maps `x=spot,y=pnl,x_format=number,y_format=number,chart_type=line`, with vlines from `input.spot` and `input.strike`; return 41 evenly spaced spots from `0.8*S` to `1.2*S` with `pnl=swept_price-current_price`. Reject nonpositive S/K/T/sigma. Let me know if frictions.

---

Before publishing, set `review = review_dashboard(FOLDER)`, inspect its one-line-per-panel receipt and every flagged `review.panel(panel_id)`, call `acknowledge_dashboard_review(FOLDER, expected_review_signature=review.review_signature, rationale="<specific acceptance rationale>")`, then call `build_dashboard(FOLDER, expected_current_version_id=... when the existing-version guard requires it)`; repair and re-review `BLOCK` instead of acknowledging it.
Build an `nfp_with_real_wages_tool` dashboard with canonical tabs `release_summary,wages_inflation,birth_death,sector_detail` and initial metadata methodology exact string `## Sources\n* Haver labor, wages, CPI, birth/death, and sector series.\n## Construction\n* Charts and KPIs use persisted source columns.`. Use exact PULLS: `pull_release` Haver codes `["USEMPNFP@USECON","USEMPPRI@USECON","LR@USECON"]` → `date:string,nfp_thousands:float64,private_payrolls_thousands:float64,unemployment_pct:float64`; `pull_wages` codes `["AHEMOM@USECON","AHEYOY@USECON","PCU@USECON"]` → `date:string,ahe_mom_pct:float64,ahe_yoy_pct:float64,cpi_yoy_pct:float64`; `pull_birth_death` code `["USBIRTHDEATH@USECON"]` → `date:string,birth_death_thousands:float64`; `pull_sector` codes `["AHEMFG@USECON","AHECONS@USECON","AHETTU@USECON"]` → `date:string,ahe_mfg_usd_hr:float64,ahe_cons_usd_hr:float64,ahe_ttu_usd_hr:float64`. Each uses `start="2015-01-01"`, its PULLS key as `name`, and standard output/session arguments; keep `TRANSFORMS=[]` and populate each tab. Then add tool `real_wages_calculator` through one typed manifest transaction: first read/parse `data/wages.csv`, sort by `date`, validate the latest row, and bake `float(latest["ahe_mom_pct"])`, `float(latest["ahe_yoy_pct"])`, and `float(latest["cpi_yoy_pct"])` into matching literal input defaults. Stat-grid outputs are `real_wage_mom_pct=ahe_mom_pct-cpi_yoy_pct/12` and `real_wage_yoy_pct=ahe_yoy_pct-cpi_yoy_pct`. Inspect current methodology, set `merged=current.rstrip()+"\n\n## Real-wages calculator\n* Defaults are the latest verified wages/CPI row at authoring time.\n* Monthly real wage proxy subtracts CPI YoY divided by 12; YoY real wage subtracts CPI YoY."`, then patch the complete merged methodology and complete tool_def at the inspected SHA. Prove PULLS/TRANSFORMS byte-identical. Let me know if frictions.

---

Before publishing, set `review = review_dashboard(FOLDER)`, inspect its one-line-per-panel receipt and every flagged `review.panel(panel_id)`, call `acknowledge_dashboard_review(FOLDER, expected_review_signature=review.review_signature, rationale="<specific acceptance rationale>")`, then call `build_dashboard(FOLDER, expected_current_version_id=... when the existing-version guard requires it)`; repair and re-review `BLOCK` instead of acknowledging it.
Build a `tips_rv_dashboard` from exact supplied call `tips_raw=bond_client.get_tips_relative_value(as_of="2026-07-10", fields=["cusip","maturity_date","z_spread_bp","modified_duration","carry_3m_bp","roll_3m_bp","nominal_cusip","breakeven_bp"])`, returning `cusip,maturity_date,nominal_cusip:string` and five measures as `float64`; persist as `tips_raw`. Set `PULLS={"tips_raw":pull_tips_raw}` and `TRANSFORMS=[derive_maturity_year]`, which emits `tips_rv` with all source fields plus `maturity_year:int64=pd.to_datetime(maturity_date).dt.year`. Set initial root title `TIPS RV Dashboard`, methodology `## Sources\n* User-supplied TIPS relative-value client snapshot dated 2026-07-10.\n## Construction\n* Z-spread versus modified duration; carry and roll shown over three months.`, and tags `["tips","relative-value","inflation"]`. Add slider `max_maturity` with bare field `maturity_year` and targets `["zspread_duration","bond_details","carry_roll","tips_nominal"]`, plus those four widgets. Then use typed metadata operations: inspect SHA/current complete methodology, set `merged=current.rstrip()+"\n* Funding assumption: SOFR + 9bp."`, and submit one transaction using `patch_dashboard` only for root `title="TIPS Relative Value Monitor"` and `patch_metadata` with complete `{"methodology":merged}`. Rebuild and verify title, exact merged methodology, widgets, filter, dataset, and provenance. Let me know if frictions.
