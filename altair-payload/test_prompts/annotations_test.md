Build a multi_line of US 10y yields over the last 5 years with a VLine at every FOMC meeting date and an HLine at the current level. Let me know if frictions.

---

Build a multi_line of US ISM Manufacturing PMI over the last 10 years with a Band shading the recession periods (NBER dates) and an Arrow pointing at the post-COVID rebound. Let me know if frictions.

---

Build a scatter of US payrolls vs unemployment rate (Phillips-curve framing) with three `PointLabel` annotations on the most recent 3 months and a `PointHighlight` on the latest data point. Let me know if frictions.

---

Build a multi_line of EUR/USD over the last 2 years with a Callout near the SVB-failure date explaining the dollar-funding stress reaction. Let me know if frictions.

---

Build a multi_line of US 2y/5y/10y/30y Treasury yields over the last year, rely on the engine's automatic end-of-line labels, and add a `PlotText(position='right')` annotation of at most eight words summarizing curve shape. Let me know if frictions.

---

Build a scatter of MOVE vs VIX with a `regression` layer overlay and a `rule` layer at VIX = 20. Let me know if frictions.

---

Build a multi_line of EU vs US 10y yields with a `Segment` whose endpoints are explicitly the US 10y May 2020 trough and US 10y October 2023 peak, to highlight that series' cycle. Let me know if frictions.
