Compile a dashboard with a row of KPI tiles showing latest value, day-over-day change, week-over-week change, and YTD change for the US 10y yield. Use the `latest`, `prev`, and `first` aggregators with delta formatting. Let me know if frictions.

---

Compile a dashboard with a `table` widget showing G10 currency-pair returns formatted as: pair (text), spot (number, 4 decimals), 1d change (bps signed), YTD (percent signed), and a delta column with up/down arrows. Let me know if frictions.

---

Compile a dashboard with a `pivot` widget showing macro-indicator releases pivoted by region (rows) and indicator type (cols), with cells showing the latest YoY value. Let me know if frictions.

---

Compile a dashboard with a `stat_grid` widget of 8 KPIs (4x2): US/EU/UK/JP on rows; CPI YoY and 10y yield on cols. Each cell shows latest value plus 1m change. Let me know if frictions.

---

Compile a dashboard with note widgets of all 6 kinds (`insight`, `thesis`, `watch`, `risk`, `context`, `fact`) on the same tab so the visual distinction between kinds is clear. Let me know if frictions.

---

Compile a dashboard with a `markdown` widget rendering a curated commentary block, an `image` widget showing a static asset (e.g. a Federal Reserve logo), and `divider` widgets separating logical sections. Let me know if frictions.

---

Compile a dashboard mixing every widget kind on a single overview tab: chart, KPI row, table, pivot, stat_grid, tool, note, markdown, image, divider. Verify they all render in the same MECE-tab style. Let me know if frictions.
