Compile a 1-tab dashboard with a single `multi_line` chart of US 2y/5y/10y/30y Treasury yields over the last 5 years. Lead the response with the portal URL. Let me know if frictions.

---

Compile a dashboard with a `correlation_matrix` chart of cross-asset returns (SPX, NDX, USD index, Brent, gold, US 10y) over the last 3 years and a click-through drawer showing the underlying scatter for each cell. Let me know if frictions.

---

Compile a dashboard with a `candlestick` chart of front-month WTI futures (last year, daily bars) and a brush selector wired to a sibling KPI tile showing the in-brush mean return. Let me know if frictions.

---

Compile a dashboard with a `heatmap` of monthly S&P 500 returns (rows = years, cols = months) AND a `calendar_heatmap` of the same data for the last 3 years. Confirm both render consistently. Let me know if frictions.

---

Compile a dashboard with a `slope` chart showing G10 currency YTD returns at the start vs latest (slope between two points). Add a `radar` chart on the same tab showing relative carry, momentum, and value scores per currency. Let me know if frictions.

---

Compile a dashboard with a `waterfall` chart of US Q1 GDP contributions by component (consumption, investment, government, net exports). Add a `marimekko` chart on the next row showing US sector weights x sector returns. Let me know if frictions.

---

Compile a dashboard with a `fan_cone` chart showing the Fed's SEP forecast distribution for the policy rate over the next 3 years (median + uncertainty bands). Add a `gauge` widget showing today's market-implied terminal rate vs the SEP median. Let me know if frictions.
