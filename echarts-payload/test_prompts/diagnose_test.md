After selecting the exact saved target, obtain `review = review_dashboard(FOLDER, version_id=target["version_id"])` and inspect its one-line-per-panel receipt plus every flagged `review.panel(panel_id)`; call `acknowledge_dashboard_review(FOLDER, expected_review_signature=review.review_signature, rationale="<specific acceptance rationale>", version_id=target["version_id"])` and only then `restore_dashboard_version(FOLDER, version_id=target["version_id"], expected_current_version_id=versions["current_version_id"])`; `BLOCK` must be repaired or another saved definition selected.
Diagnose inherited dashboard `blank_rates` without mutation. Start only with diagnosis context and public `inspect_dashboard`. The expected evidence scenario is: required template/build/pull files are present; optional telemetry is present; canonical tab `overview` contains chart id `rates_chart` referencing dataset `rates`; `data/rates.csv` is absent; dataset producer classification is `pull`; PULLS contains key `rates`; latest refresh evidence says that pull failed; registry has exactly one aligned entry; telemetry has no causal browser error. Use files/findings/tabs/datasets/widgets/graph/registry/telemetry fields to determine the cause rather than assuming it. Report cause `missing persisted rates attachment after failed pull`, repair owner `pull_data.py / PULLS["rates"]`, and exact lawful next action `run_pull(folder,"rates"), verify persisted schema, strict build, clean refresh, inspect again`; do not execute it in this diagnosis-only prompt. Let me know if frictions.

---

After diagnosis, obtain `review = review_dashboard(FOLDER)` and inspect its one-line-per-panel receipt plus every flagged `review.panel(panel_id)`; when this prompt authorizes publication, call `acknowledge_dashboard_review(FOLDER, expected_review_signature=review.review_signature, rationale="<specific acceptance rationale>")` and then `build_dashboard(FOLDER, expected_current_version_id=... when the existing-version guard requires it)`, while read-only or external-owner cases must report that exact held sequence without executing it and `BLOCK` must be repaired.
Diagnose `orphan_rates`, where strict compile succeeds, clean refresh fails, chart id `us10y_history` consumes dataset `rates`, inspection classifies its existing `data/rates.csv` as `persisted_only` with no pull/transform producer, and the CSV is older than the requested refresh cadence. Trace `us10y_history → rates → data/rates.csv → no producer` using inspection. Repair the durable owner by adding exact `PULLS["rates"]=pull_rates`, whose body calls `pull_plottool_data(expressions=["sofrswp10y"], labels=["us_10y_pct"], start="2015-01-01", name="rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)` and returns `date:string,us_10y_pct:float64`; keep `TRANSFORMS=[]`. Run only rates, verify schema/provenance, strict build/clean refresh, and inspect until producer classification is `pull`, attachment exists, consumer resolves, and refresh is clean. Do not edit generated CSV/HTML directly. Let me know if frictions.

---

After diagnosis, obtain `review = review_dashboard(FOLDER)` and inspect its one-line-per-panel receipt plus every flagged `review.panel(panel_id)`; when this prompt authorizes publication, call `acknowledge_dashboard_review(FOLDER, expected_review_signature=review.review_signature, rationale="<specific acceptance rationale>")` and then `build_dashboard(FOLDER, expected_current_version_id=... when the existing-version guard requires it)`, while read-only or external-owner cases must report that exact held sequence without executing it and `BLOCK` must be repaired.
Diagnose `bond_popup_blank`. Structured popup findings should show table id `bond_table` over `bonds` with `row_key="cusip"`, detail chart dataset `bond_history`, but `filter_field="ticker"`; available source columns include `cusip,ticker`, detail columns include `date,cusip,price`, representative source/detail CUSIP overlap is non-empty, and code `popup_section_filter_field_missing` includes missing-column/available-columns/fix-hint context. After inspection identifies `bond_table`, use the evidence-identified repair owner: read its complete current object from the decoded template, deep-copy the entire current `row_click`, change only `row_click.detail.sections[0].filter_field` to `cusip`, then submit exact operation `{"op":"update_widget","selector":{"id":"bond_table"},"patch":{"row_click":<complete updated row_click>}}` at the inspected SHA. Do not patch only `detail`/`sections` and do not reshape upstream data. Rebuild/refresh, inspect until popup findings clear, then reproduce one CUSIP and confirm its 180-day line has rows. Let me know if frictions.

---

After diagnosis, obtain `review = review_dashboard(FOLDER)` and inspect its one-line-per-panel receipt plus every flagged `review.panel(panel_id)`; when this prompt authorizes publication, call `acknowledge_dashboard_review(FOLDER, expected_review_signature=review.review_signature, rationale="<specific acceptance rationale>")` and then `build_dashboard(FOLDER, expected_current_version_id=... when the existing-version guard requires it)`, while read-only or external-owner cases must report that exact held sequence without executing it and `BLOCK` must be repaired.
Diagnose `resource_path_mismatch`, whose latest refresh says success but telemetry contains recent event `{"kind":"resource_404","url":"/dashboards/resource_path_mismatch/data/rates_old.csv","message":"404","source":"browser"}` classified `resource`. Inspection shows chart `rates_chart` consumes dataset `rates`, graph stem is `rates`, `persisted_path_index.by_relative_path` contains `data/rates.csv` and not `data/rates_old.csv`, and the inherited template slot incorrectly has string `source:"data/rates_old.csv"` instead of header-only template source. Normalize the event URL and use `by_relative_path` as the first comparator, then basename only if needed. Repair with exact typed operation `{"op":"update_dataset","name":"rates","patch":{"source":[["date","us_10y_pct"]],"template":true}}` at the inspected SHA; the next build populates slot `rates` from canonical `data/rates.csv`. Do not invent `source_path`/`csv_path`, edit HTML, or copy/rename CSV. Strict build/clean refresh, inspect, and capture new browser evidence showing no current 404 for the old URL. Let me know if frictions.

---

After diagnosis, obtain `review = review_dashboard(FOLDER)` and inspect its one-line-per-panel receipt plus every flagged `review.panel(panel_id)`; when this prompt authorizes publication, call `acknowledge_dashboard_review(FOLDER, expected_review_signature=review.review_signature, rationale="<specific acceptance rationale>")` and then `build_dashboard(FOLDER, expected_current_version_id=... when the existing-version guard requires it)`, while read-only or external-owner cases must report that exact held sequence without executing it and `BLOCK` must be repaired.
Diagnose `duplicate_registry_monitor`, where inspection reports `registry.match_count=2`, a canonical entry with `created_at="2026-07-01T12:00:00Z"` and refresh `1h`, a duplicate entry with refresh `1d`, template refresh `1h`, and `registry.manifest_pointer.state="stale"`. Preserve canonical entry/created_at. Attempt no invented client mutation: the public dashboard API may synchronize cadence only when exactly one registry match exists and inspection's pointer is read-only evidence. Report exact external action to the Registry owner: remove the duplicate entry for dashboard id `duplicate_registry_monitor`, preserve the canonical entry and its `created_at`, and update that user's manifest pointer to the canonical manifest path. Include evidence identifying both entries/pointer state and state that cadence sync should be rerun only after the external owner confirms one match. Do not claim repair or aligned final inspection without that confirmation. Let me know if frictions.

---

After diagnosis, obtain `review = review_dashboard(FOLDER)` and inspect its one-line-per-panel receipt plus every flagged `review.panel(panel_id)`; when this prompt authorizes publication, call `acknowledge_dashboard_review(FOLDER, expected_review_signature=review.review_signature, rationale="<specific acceptance rationale>")` and then `build_dashboard(FOLDER, expected_current_version_id=... when the existing-version guard requires it)`, while read-only or external-owner cases must report that exact held sequence without executing it and `BLOCK` must be repaired.
Diagnose `viewer_specific_error` without mutation. Inspection evidence is: required files present; all displayed datasets attached; latest refresh clean; registry has one aligned entry; popup/findings clear; most viewers have no errors; the affected viewer's only recent event is `{"kind":"runtime_error","message":"ResizeObserver loop limit exceeded","url":"chrome-extension://viewer-helper/content.js","source":"window.onerror"}`, which normalizes to category `extension_or_viewer`. Separate that from dashboard_runtime/resource/csp evidence and do not change the product without a causal dashboard finding. Report in product language: `The dashboard and data pipeline are healthy; this viewer's browser extension or embedded viewer is generating the error. Retry in a clean browser profile or disable the extension, then capture new telemetry if the chart remains affected.` Let me know if frictions.

---

After diagnosis, obtain `review = review_dashboard(FOLDER)` and inspect its one-line-per-panel receipt plus every flagged `review.panel(panel_id)`; when this prompt authorizes publication, call `acknowledge_dashboard_review(FOLDER, expected_review_signature=review.review_signature, rationale="<specific acceptance rationale>")` and then `build_dashboard(FOLDER, expected_current_version_id=... when the existing-version guard requires it)`, while read-only or external-owner cases must report that exact held sequence without executing it and `BLOCK` must be repaired.
The user asks about `rates_revert`: “Can we go back to the version from yesterday?” Start with `list_dashboard_versions`, passing the user’s IANA timezone; exactly one saved definition has `local_date` in the user’s yesterday, while the current version added `PULLS["macro"]`, transform `derive_macro`, dataset `macro`, and widgets `macro_chart,macro_kpi`. Re-list immediately before mutation, then restore the exact listed yesterday version with `restore_dashboard_version` and its required expected-current id. Verify `inspect_dashboard.versioning.clean`, PULLS exactly `["rates"]`, empty TRANSFORMS, dataset `rates`, widgets `curve_history,kpi_2y,kpi_10y`, and no macro surface. Do not reconstruct prior bytes or edit history objects. Tell the user the earlier dashboard definition is restored and uses the latest available data. If the timezone is unavailable or multiple versions match yesterday, ask instead of guessing. Let me know if frictions.
