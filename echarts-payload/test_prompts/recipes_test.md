Build an `nfp_wage_inflation_redesign` dashboard. `PULLS={"ahe_sector":pull_ahe_sector}` calls exact verified Haver codes `["USEMPNFP@USECON","USEMPPRI@USECON","LR@USECON","AHEYOY@USECON","AHEMFGYOY@USECON","AHECONSYOY@USECON","AHETTUYOY@USECON","PCEPMFG_L6@USECON","PCEPCONS_L6@USECON","PCEPTTU_L6@USECON"]` via `pull_haver_data(..., start="2010-01-01", name="ahe_sector", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` plus `nfp_thousands,private_payrolls_thousands,unemployment_pct,ahe_yoy_pct,ahe_mfg_yoy_pct,ahe_cons_yoy_pct,ahe_ttu_yoy_pct,pce_mfg_lag6_yoy_pct,pce_cons_lag6_yoy_pct,pce_ttu_lag6_yoy_pct` as `float64`. Start `TRANSFORMS=[]`; KPIs are `nfp,private_payrolls,unemployment,ahe_yoy`; table `release_summary` consumes `ahe_sector` with exact columns `date,nfp_thousands,private_payrolls_thousands,unemployment_pct,ahe_yoy_pct`; initial sector labels are `Manufacturing`, `Construction`, `Trade, Transportation & Utilities`. Extend the same pull—no second pull—with codes `["PCEPMFG_L3@USECON","PCEPCONS_L3@USECON","PCEPTTU_L3@USECON","PCEPMFG_L9@USECON","PCEPCONS_L9@USECON","PCEPTTU_L9@USECON","PCEPMFG_L12@USECON","PCEPCONS_L12@USECON","PCEPTTU_L12@USECON"]` returning exact matching `pce_<mfg|cons|ttu>_lag<3|9|12>_yoy_pct:float64`, then set `TRANSFORMS=[derive_ahe_pce_pairs]`. The transform parses/sorts/deduplicates `date`; maps each exact sector label to its AHE column and four supplied PCE lag columns; for each sector/lag `[3,6,9,12]` drops rows missing either value, selects the latest common row, computes `gap_pct_points=AHE-PCE`, and emits two rows labelled `AHE YoY` and `PCE YoY`. Combined `ahe_pce_pairs` schema is `date:datetime64[ns],sector:string,sector_label:string,lag_months:int64,series:string,value_pct:float64,gap_pct_points:float64`. It also projects identical-schema datasets `ahe_pce_manufacturing`, `ahe_pce_construction`, `ahe_pce_trade_transport_utilities`. Add bar-horizontal charts: `manufacturing_wage_pce` uses `ahe_pce_manufacturing`, `construction_wage_pce` uses `ahe_pce_construction`, `ttu_wage_pce` uses `ahe_pce_trade_transport_utilities`; every mapping is `x=value_pct,y=series`. Select `lag` uses bare field `lag_months`, options `[3,6,9,12]`, default 6, and exact targets all three chart ids. Let me know if frictions.

---

Build a `rates_monitor` dashboard with `PULLS={"rates":pull_rates}` using exact call `pull_plottool_data(expressions=["sofrswp2y","sofrswp10y","sofrswp30y"], labels=["us_2y_pct","us_10y_pct","us_30y_pct"], start="2000-01-01", name="rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` and three `float64` percent fields. Start with `TRANSFORMS=[]` and chart ids `us2y_curve,us10y_curve,us30y_curve` in one row. Then leave `pull_data.py` byte-identical and add `derive_realized_vol` to `TRANSFORMS`. It must parse/sort/deduplicate date, compute daily changes in basis points as `diff()*100`, then for each tenor compute `rolling(window=1260,min_periods=1260).std(ddof=1)*sqrt(252)`; emit `rates_realized_vol` columns `date:datetime64[ns],us_2y_rv_5y_bp:float64,us_10y_rv_5y_bp:float64,us_30y_rv_5y_bp:float64`. Add one three-series chart id `rolling_rv_5y` below the originals, y-axis `Annualized volatility (bp)`, and computed provenance carrying formula/lookback/annualization/upstream columns. Let me know if frictions.

---

Build an `nfp_wages_revert` dashboard with initial `PULLS={"ahe_sector":pull_ahe_sector}` calling `pull_haver_data(codes=["AHEMFG@USECON","AHECONS@USECON","AHETTU@USECON"], start="2010-01-01", name="ahe_sector", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string,ahe_mfg_usd_hr:float64,ahe_cons_usd_hr:float64,ahe_ttu_usd_hr:float64`; set `TRANSFORMS=[]` and build three sector small multiples labelled `Manufacturing`, `Construction`, `Trade, Transportation & Utilities`. Retain exact pre-redesign template/pull/build bytes. Redesign by adding `pull_pce_lagged`, exact call `pull_haver_data(codes=["PCEPMFG@USECON","PCEPCONS@USECON","PCEPTTU@USECON"], start="2010-01-01", name="pce_lagged", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string,pce_mfg_index:float64,pce_cons_index:float64,pce_ttu_index:float64`, plus `PULLS["pce_lagged"]`. Set `TRANSFORMS=[derive_lag_pairs]`. The transform inner-joins monthly rows on parsed/sorted/deduplicated date; computes each AHE level's `pct_change(12)*100` and each exact PCE index's `pct_change(12)*100`; for each sector and lag `[3,6,9,12]`, shifts that sector's PCE YoY by `lag`, takes the latest row where AHE/PCE are both finite, computes `gap_pct_points=AHE-PCE`, and emits two rows `AHE YoY`/`PCE YoY`. Emit combined schema `date:datetime64[ns],sector:string,sector_label:string,lag_months:int64,series:string,value_pct:float64,gap_pct_points:float64` and identical-schema subsets `lag_pairs_manufacturing`, `lag_pairs_construction`, `lag_pairs_trade_transport_utilities`. Bar-horizontal charts `manufacturing_wage_pce`, `construction_wage_pce`, `ttu_wage_pce` use those respective datasets with `x=value_pct,y=series`; select `lag` has bare field `lag_months`, options `[3,6,9,12]`, default 6, and targets all three. Then inspect and revert through owning paths to exact retained bytes, run affected pulls, strict build/refresh, and verify PULLS exactly `["ahe_sector"]`, empty TRANSFORMS, and no redesign surface. Let me know if frictions.

---

Diagnose a `fed_scenario_tool` dashboard whose current canonical tab order is only `["visualization"]`, while retained known-good template bytes contain tab `{"id":"scenario_builder","label":"Scenario Builder"}` at index 0 and tool widget id `fed_scenario_inputs` with a complete retained `tool_def`. Treat the retained tab/widget objects as authoritative product state; do not recreate or reinterpret `compute_js`. Inspect current SHA and verify the tab/widget are absent, then restore the exact retained tab at index 0 and exact retained tool widget at row 0 index 0 through one typed manifest transaction. Require PULLS/TRANSFORMS and existing Visualization widgets byte-equivalent, rebuild/refresh, and inspect tab order plus widget placement and exact tool-def equality. Let me know if frictions.

---

Build a `us_europe_curves` dashboard with two exact pull entries. `pull_us_curve` calls `pull_plottool_data(expressions=["sofrswp2y","sofrswp5y","sofrswp10y","sofrswp30y"], labels=["US2Y","US5Y","US10Y","US30Y"], start="2010-01-01", name="us_curve", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)` and returns `date:string,US2Y:float64,US5Y:float64,US10Y:float64,US30Y:float64`. `pull_de_curve` calls `pull_haver_data(codes=["DE2Y@EUROPE","DE5Y@EUROPE","DE10Y@EUROPE","DE30Y@EUROPE"], start="2010-01-01", name="de_curve", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)` and returns exact labels `date:string,DE2Y:float64,DE5Y:float64,DE10Y:float64,DE30Y:float64`. Set `TRANSFORMS=[derive_us_curve_long,derive_de_curve_long]`. Each transform parses/sorts `date` and melts only its own four columns: US maps `US2Y/US5Y/US10Y/US30Y` to tenors `2Y/5Y/10Y/30Y`; DE maps `DE2Y/DE5Y/DE10Y/DE30Y` to the same tenor labels. Outputs are `us_curve_long(date:datetime64[ns],tenor:string,yield_pct:float64)` and `de_curve_long` with identical schema. Tabs `us_rates` and `europe_rates` each contain exact thesis text (`US: Monitor whether long-end yields rise faster than front-end yields.` / `Germany: Monitor whether Bund long-end yields rise faster than front-end yields.`) and one four-series chart mapping its own long dataset `x=date,y=yield_pct,color=tenor`. Do not assert an uncomputed regime. Add exact source and per-transform reshape provenance. Let me know if frictions.

---

Build a `tips_rv_screener` dashboard with `PULLS={"tips":pull_tips}` using exact verified expressions `["TIPS_91282CGW5_ZSPREAD","TIPS_91282CKL4_ZSPREAD","TIPS_91282CHP9_ZSPREAD","TIPS_912810TV0_ZSPREAD"]` and labels `["cusip_91282CGW5","cusip_91282CKL4","cusip_91282CHP9","cusip_912810TV0"]` in `pull_plottool_data(..., start="2019-01-01", name="tips", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` plus four `float64` z-spread-bp columns. `TRANSFORMS=[derive_tips_rv]` melts to CUSIP history, joins the supplied maturity-year map `{91282CGW5:2027,91282CKL4:2029,91282CHP9:2032,912810TV0:2044}`, and for each CUSIP uses the last five calendar years through its own last date. Require at least two observations and nonzero sample std; emit `tips_rv` columns `cusip:string,as_of:datetime64[ns],maturity_year:int64,current_z_spread_bp:float64,low_5y_bp:float64,high_5y_bp:float64,mean_5y_bp:float64,std_5y_bp:float64,percentile_5y_pct:float64,zscore_5y:float64`, with percentile `100*mean(window<=current)` and z-score `(current-mean)/std(ddof=1)`. Render one bullet row per issue plus sibling table, both filtered by slider id `max_maturity_year`, min 2027, max 2044, default 2044. Add exact source and computed provenance. Let me know if frictions.

---

Build a `curve_monitor` dashboard with `PULLS={"rates":pull_rates}` using exact call `pull_plottool_data(expressions=["sofrswp2y","sofrswp10y"], labels=["us_2y_pct","us_10y_pct"], start="2015-01-01", name="rates", output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)`, returning `date:string` and two `float64` percent fields. Add `TRANSFORMS=[derive_spread]`; it parses/sorts/deduplicates date, computes `(us_10y_pct-us_2y_pct)*100`, and writes `spread` columns `date:datetime64[ns],spread_2s10s_bp:float64`. Top row contains thesis card id `thesis` with exact text `Thesis: monitor whether the 2s10s curve continues to steepen.` and watch card id `watch` with exact text `Watch: front-end repricing and changes in the 10Y term premium.` Second row is 2-up: chart id `curve_level` over dataset key `rates`, two series; chart id `spread_2s10s` over dataset key `spread`, one series, units bp. Put the exact pull identifiers and formula/upstream fields in dataset-entry provenance. Let me know if frictions.
