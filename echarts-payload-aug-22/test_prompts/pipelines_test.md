Build a `three_source_macro` dashboard whose `scripts/pulls.json` declares exactly these three entries in this order through one `apply_pulls_document` write: `{"name":"rates","details":{"source":"tsdb_eod","start":"2015-01-01","end":"@today","symbols":[{"symbol":"sofrswp2y","label":"us_2y_pct"},{"symbol":"sofrswp10y","label":"us_10y_pct"}]}}` → `date:string` plus two `float64` percent fields; `{"name":"cpi","details":{"source":"haver","start":"2015-01-01","symbols":[{"code":"JCXFE@USECON","label":"core_pce_yoy_pct"}, {"code":"PCU@USECON","label":"headline_cpi_yoy_pct"}]}}` → `date:string,core_pce_yoy_pct:float64,headline_cpi_yoy_pct:float64`; `{"name":"labor","details":{"source":"client","calls":[{"client":"fred","function":"pull_fred_data","arguments":{"codes":["UNRATE"],"start":"2015-01-01"},"label":"unrate"}]}}` → the client's own schema, `date:string,UNRATE:float64`, since a single `source="client"` call is not relabelled. Keep `TRANSFORMS=[]`. Across tab ids `rates` and `macro`, create exactly six consumers: chart `rates_history`; KPIs `kpi_2y` and `kpi_10y`; chart `inflation_history`; chart `unemployment_history`; and stat_grid `latest_inflation` with stats `[{"id":"core_pce","label":"Core PCE YoY","source":"cpi.latest.core_pce_yoy_pct","suffix":"%","decimals":2},{"id":"headline_cpi","label":"Headline CPI YoY","source":"cpi.latest.headline_cpi_yoy_pct","suffix":"%","decimals":2}]`. Add source field provenance under each dataset. Once the build is live, call `inspect_dashboard` and report exact `pull entry name → declared stem → dataset key → consumer ids` edges plus the three declared stems and empty TRANSFORMS. Let me know if frictions.

---

Build a `rates_distribution_edit` dashboard whose `scripts/pulls.json` declares exactly one entry `{"name":"rates","details":{"source":"tsdb_eod","start":"2015-01-01","end":"@today","symbols":[{"symbol":"sofrswp2y","label":"us_2y_pct"},{"symbol":"sofrswp5y","label":"us_5y_pct"},{"symbol":"sofrswp10y","label":"us_10y_pct"},{"symbol":"sofrswp30y","label":"us_30y_pct"}]}}`, returning `date:string` plus four `float64` percent fields. Set `TRANSFORMS=[]`. Feed one four-line chart id `curve_history` and KPI ids `kpi_2y,kpi_5y,kpi_10y,kpi_30y`. Then do not edit the pull document: inspect current template SHA and dataset columns, add histogram id `us10y_distribution` over existing `rates.us_10y_pct` to the bottom row through one typed `add_widget` transaction, rebuild/refresh, and verify the new consumer while preserving every existing field and widget and leaving `scripts/pulls.json` byte-identical at its inspected `sha256`. Let me know if frictions.

---

Build a `rates_extend_existing_pull` dashboard whose `scripts/pulls.json` declares one initial entry `{"name":"rates","details":{"source":"tsdb_eod","start":"2015-01-01","end":"@today","symbols":[{"symbol":"sofrswp2y","label":"us_2y_pct"},{"symbol":"sofrswp10y","label":"us_10y_pct"}]}}`, returning `date:string` and two `float64` percent fields. Set `TRANSFORMS=[]` and add chart ids `us2y_history` and `us10y_history`. Then add 5Y by reading `state["scripts"]["pulls"]["document"]` and mutating only that entry's `symbols` list to `[{"symbol":"sofrswp2y","label":"us_2y_pct"},{"symbol":"sofrswp5y","label":"us_5y_pct"},{"symbol":"sofrswp10y","label":"us_10y_pct"}]` before one guarded `apply_pulls_document` write; require a changed `sha256`, unchanged `name`, `source`, `start` and `end`, and byte-identical `declared_stems`. Run `run_pull(folder,"rates")`, prove `rates.csv` now contains `us_5y_pct:float64` while preserving the other columns, then add chart id `us5y_history` without creating another pull entry. Let me know if frictions.

---

Build a `rates_with_gs_bank` dashboard whose initial `scripts/pulls.json` declares one entry `{"name":"rates","details":{"source":"tsdb_eod","start":"2015-01-01","end":"@today","symbols":[{"symbol":"sofrswp2y","label":"us_2y_pct"},{"symbol":"sofrswp10y","label":"us_10y_pct"}]}}` → `date:string,us_2y_pct:float64,us_10y_pct:float64`, with `TRANSFORMS=[]` and two rates charts. Then append a genuinely new second entry named `gs_bank` reaching FDIC through `{"source":"client","calls":[{"client":"fdic", ...}]}` — read the FDIC guide to pick the right public function and its keyword names, and tell me which you chose rather than guessing. The query is Goldman's bank: filters `CERT:33124`, fields `REPDTE,ASSET,DEP,NIMY,ROA`, limit 8, sorted by `REPDTE` descending. Write the whole document back through one guarded `apply_pulls_document` call requiring a changed `sha256` and `declared_stems` exactly `["rates","gs_bank"]`, then run only that pull and report `gs_bank.csv`'s actual columns and dtypes as the client returned them — `source="client"` is untrusted and relabels nothing, so do not assume a schema. Reshaping is derivation, not retrieval: add `derive_gs_bank_quarterly` to `TRANSFORMS` to read `datasets["gs_bank"]`, rename to `report_date,assets_usd_thousands,deposits_usd_thousands,net_interest_margin_pct,return_on_assets_pct`, cast the date to string and the four measures to float64, sort ascending, and materialize dataset `gs_bank_quarterly`. Verify exactly eight quarters, then render assets/deposits in explicit two-chart small multiples plus one two-series NIM/ROA chart, all consuming `gs_bank_quarterly`. Let me know if frictions.

---

Build a `rates_dependency_removal` dashboard whose `scripts/pulls.json` declares one entry `{"name":"rates","details":{"source":"tsdb_eod","start":"2015-01-01","end":"@today","symbols":[{"symbol":"sofrswp2y","label":"us_2y_pct"},{"symbol":"sofrswp5y","label":"us_5y_pct"},{"symbol":"sofrswp10y","label":"us_10y_pct"}]}}` → `date:string` plus three `float64` percent fields. Set `TRANSFORMS=[derive_curve_long,derive_spread]`: `derive_curve_long` parses/sorts `date`, melts exact value vars `["us_2y_pct","us_5y_pct","us_10y_pct"]`, maps them to tenor labels `["2Y","5Y","10Y"]`, and emits `curve_long(date:datetime64[ns],tenor:string,yield_pct:float64)`; `derive_spread` emits `spread(date:datetime64[ns],spread_2s10s_bp:float64)` with `(us_10y_pct-us_2y_pct)*100`. Chart `curve_history` maps `curve_long` as `x=date,y=yield_pct,color=tenor`; table `curve_table` consumes `curve_long`; select `tenor_filter` has bare field `tenor`, options `["2Y","5Y","10Y"]`, default `10Y`, targets `["curve_history","curve_table"]`. Chart `spread_2s10s` consumes `spread`; KPI ids `kpi_2y,kpi_10y` consume `rates`. Then remove 2Y entirely. Before applying the destructive change, call `inspect_dashboard` and enumerate the 2Y pull symbol/label, `derive_curve_long` value-var/label, `derive_spread`, `rates/curve_long/spread` columns, `curve_history,curve_table,spread_2s10s,kpi_2y`, and filter option `2Y`. Remove `{"symbol":"sofrswp2y","label":"us_2y_pct"}` from that entry's `symbols` list through one guarded `apply_pulls_document` write, then remove the 2Y long mapping, 2s10s transform/dataset/chart, KPI `kpi_2y`, and option `2Y`; preserve 5Y/10Y curve surfaces and `kpi_10y`. Run the affected pull, strict build/refresh, and prove no stale 2Y reference remains. Let me know if frictions.

---

Build a `rates_macro_script_edit` dashboard whose initial `scripts/pulls.json` declares one entry `{"name":"rates","details":{"source":"tsdb_eod","start":"2015-01-01","end":"@today","symbols":[{"symbol":"sofrswp2y","label":"us_2y_pct"},{"symbol":"sofrswp10y","label":"us_10y_pct"}]}}` → `date:string` plus two `float64` fields, `TRANSFORMS=[]`, and chart ids `us2y_history,us10y_history`. Then read `state["scripts"]["pulls"]["document"]` and append exactly one entry `{"name":"macro","details":{"source":"haver","start":"2015-01-01","symbols":[{"code":"LR@USECON","label":"unemployment_rate_pct"}, {"code":"JCXFE@USECON","label":"core_pce_yoy_pct"}]}}`, producing `date:string,unemployment_rate_pct:float64,core_pce_yoy_pct:float64`, and write the whole document back with one guarded `apply_pulls_document` call. Require a changed `sha256`, `declared_stems` exactly `["rates","macro"]`, and the pre-existing rates entry byte-identical; run only macro, verify schema, then add two-series chart id `macro_history` consuming the new dataset and verify changed compiled output. Let me know if frictions.

---

Build a `pipeline_edge_audit` dashboard whose initial `scripts/pulls.json` declares exact entry `{"name":"rates","details":{"source":"tsdb_eod","start":"2015-01-01","end":"@today","symbols":[{"symbol":"sofrswp2y","label":"us_2y_pct"},{"symbol":"sofrswp10y","label":"us_10y_pct"}]}}` → `rates.csv` columns `date:string,us_2y_pct:float64,us_10y_pct:float64`; `TRANSFORMS=[]`; and charts `us2y_history,us10y_history`. Add exact Haver entry `{"name":"macro","details":{"source":"haver","start":"2015-01-01","symbols":[{"code":"LR@USECON","label":"unemployment_rate_pct"}, {"code":"JCXFE@USECON","label":"core_pce_yoy_pct"}]}}` → `macro.csv` columns `date:string,unemployment_rate_pct:float64,core_pce_yoy_pct:float64` through one guarded `apply_pulls_document` write, and add chart `macro_history`. After the edit, call `inspect_dashboard` and report exact edges for both entry names/declared stems/datasets/three consumers, dtypes, each `graph.pipelines[]` entry's `name`, `source` and `csv_stems`, and preserved rates columns/empty TRANSFORMS; confirm `graph.producer_analysis` exposes only `transform_unresolved` and that it is empty; then require strict build and clean refresh. Let me know if frictions.

---

Build a `factor_helper_outputs` dashboard from exact deterministic user fixtures while preserving one coherent producer. Nothing is retrieved, so persist exactly `{"schema_version": 1, "pulls": []}`. In `build.py`, define local helper `_materialize(target, key, frame)` that calls `save_artifact(frame, name=key, output_path=f"{SESSION_PATH}/data", s3_manager=s3_manager)` and then assigns `target[key]=frame`. Define one `load_factor_model(datasets)` that creates these exact frames: `factor_corr=pd.DataFrame([{"factor":"Growth","Rates":-0.4,"Credit":0.6},{"factor":"Inflation","Rates":0.7,"Credit":0.2}])`, `heat_z=pd.DataFrame([{"asset":"Rates","Growth":-1.2,"Inflation":0.8},{"asset":"Credit","Growth":0.5,"Inflation":0.3}])`, and `heat_beta=pd.DataFrame([{"asset":"Rates","Growth":-0.7,"Inflation":1.1},{"asset":"Credit","Growth":0.9,"Inflation":0.4}])`; puts them in literal mapping `outputs={"factor_corr":factor_corr,"heat_z":heat_z,"heat_beta":heat_beta}`, loops `for stem, frame in outputs.items(): _materialize(datasets, stem, frame)`, and returns datasets—do not split it into three single-output functions. Also define `derive_factor_summary(datasets)` that calls `_materialize(datasets, "factor_summary", pd.DataFrame([{"metric":"positive_beta_cells","value":3.0}]))` and returns datasets; set `TRANSFORMS=[load_factor_model,derive_factor_summary]`. Render heatmaps `zscore_heatmap` from `heat_z` and `beta_heatmap` from `heat_beta`, table `correlation_table` from `factor_corr`, and KPI `positive_beta_cells` from `factor_summary.latest.value`, with user-fixture provenance. Run strict build and clean refresh, then once the build is live call `inspect_dashboard`. Require empty `graph.pipelines`, `graph.transforms` resolving exactly `factor_corr`, `factor_summary`, `heat_beta` and `heat_z`, an empty `producer_analysis.transform_unresolved`, all four producer-to-consumer edges, and no attachment gaps. Do not add dummy assignments or unroll the literal loop to satisfy inspection. Let me know if frictions.
